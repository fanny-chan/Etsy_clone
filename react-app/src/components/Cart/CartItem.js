import React from 'react'
import styled from 'styled-components'

export default function CartItem({productId,productName,productDescription,price, quantity}) {
    return (
        <div>
            
        </div>
    )
}
